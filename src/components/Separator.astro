---
interface Props {
  img: string;
  className?: string;
}

const { img, className } = Astro.props;
---

<div class={`w-full h-auto block ${className}`}>
  <img src={`/img/${img}`} alt="separator" />
</div>
